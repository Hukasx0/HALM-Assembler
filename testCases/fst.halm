include macroMlmTest.halm

addBytes = "Hello, world!",0xA

-- simple math testing
addBytes = (times 20 (* (+ 1 3) (+ 1 3) ) )

doSh = "echo 'This is a message after compilation :D'"

-- using mlm from "macroMlmTest.halm"
use displayTxt
-- again using mlm from "macroMlmTest.halm"
use moreDispTests
